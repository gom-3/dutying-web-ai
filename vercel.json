{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --dir apps/app build",
  "outputDirectory": "apps/app/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
